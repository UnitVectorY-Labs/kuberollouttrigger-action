{
  "name": "kuberollouttrigger-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action for triggering rollout webhook with OIDC",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "bundle": "npm run build",
    "lint": "node --check src/index.js"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/http-client": "^4.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
